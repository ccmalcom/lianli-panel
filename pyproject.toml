[project]
name = "lianli-panel"
version = "0.1.0"
requires-python = ">=3.13"

[project.scripts]
lianli-panel = "lianli_panel.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
